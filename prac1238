prac1 text center and font bold
prac2  image from internet 
prac3  Image from system
prac8  Hello Applicatipon
import 'package:flutter/material.dart';
void main() {
  runApp(myApp());
}

class myApp extends StatelessWidget {
  
  
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title:title("Hello All")
        ),
  //  prac1 body:Container(alignment:Alignment.center,child: Text("Hello",style:TextStyle(fontWeight:FontWeight.bold)),)
  //  prac2  body:Container(alignment:Alignment.center,child: Image.network(""),)
  // prac3 body:Container(alignment: Alignment.center,child: Image.asset("assets/image/error.png"),)    
  //prac8 same as prac1
    )
    );
  }
}
